spring.cache.type: none
spring:
  init:
    mode: always
  liquibase:
    changeLog: 'classpath:testDb/changelog.xml'
  datasource:
    driverClassName: org.h2.Driver
    username: sa
    password: sa
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    jpa:
      hibernate:
        hbm2ddl:
          auto: create